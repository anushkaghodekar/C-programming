#include<stdio.h>
int fact(int n);
void main()
{
    int n;
    fact(n);
}

int fact(int n)
{
 int fact=1,i;
 printf("Enter the no you want to find factorial of = ");
 scanf("%d",&n);
 for(i=1;i<=n;i++)
 {
     fact=fact*i;
     
 }
 printf("the factorial is : %d",fact);
 return 0;
}
