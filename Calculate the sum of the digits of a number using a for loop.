#include <stdio.h>
void main()
{
    int num, sum = 0, i, digit;
    printf("Enter a number: ");
    scanf("%d", &num);

    for (i=0;i<=num;i++) {
        digit = num % 10; 
        sum=sum+digit;  
        num=num/10;
    }
    printf("Sum of digits = %d\n", sum);
}
