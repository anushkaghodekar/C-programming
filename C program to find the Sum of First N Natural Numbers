#include<stdio.h>
void main()
{
    int n,i,sum=0;
    printf("enter how many numbers you want to calc sum of = ");
    scanf("%d",&n);
    
    for(i=0;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("sum of %d numbers is = %d",n,sum);
}
